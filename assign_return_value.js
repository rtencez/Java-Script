// Assign with a Returned Value

var changed = 0;

function change(num) {
    return (num+5)/3;
}

changed = change(7);

var result = 0;

function sumofTwoDigit(num1, num2) {
    return num1 + num2;
}

result = sumofTwoDigit(67,7);